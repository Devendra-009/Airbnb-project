import { useEffect, useState } from "react";
import { api } from "../services/api";
import { useAuth } from "../context/AuthContext";
import toast from "react-hot-toast";
export default function Profile(){const {user,setUser}=useAuth();const [form,setForm]=useState({name:"",bio:"",phone:""});const [avatar,setAvatar]=useState(null);useEffect(()=>{if(user)setForm({name:user.name||"",bio:user.bio||"",phone:user.phone||""})},[user]);async function save(e){e.preventDefault();const fd=new FormData();Object.entries(form).forEach(([k,v])=>fd.append(k,v));if(avatar)fd.append("avatar",avatar);try{const {data}=await api.put("/users/me",fd);setUser(data.user);toast.success("Profile updated")}catch(e){toast.error(e.response?.data?.message||"Update failed")}}return <section className="container narrow"><div className="section-title"><span className="eyebrow">Account</span><h1>Your profile</h1></div><form className="panel profile-form" onSubmit={save}>{user?.avatar?<img className="avatar-large" src={user.avatar} alt="profile"/>:<div className="avatar-large placeholder">{user?.name?.[0]}</div>}<label>Profile image<input type="file" accept="image/*" onChange={e=>setAvatar(e.target.files?.[0])}/></label><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input value={user?.email||""} disabled/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Bio<textarea value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})}/></label><button className="primary">Save changes</button></form></section>}
